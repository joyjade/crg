name: field.blocks.list.name
icon: list-bullet
wysiwyg: true
preview: list
fields:
  text:
    label: field.blocks.list.name
    type: list
  columns:
    label: Columns
    type: select
    options: 
      double: "Double"
      tripe: "Triple"